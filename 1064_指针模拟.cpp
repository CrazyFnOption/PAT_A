/*
	后面有时间写一下 这里的指针模拟树，用中序遍历直接写出层序遍历
*/
#include <iostream>
#include <algorithm>
#include <cstring>
#include <string>
#include <vector>
#include <cstdio>
#include <queue>
#include <map>
#include <cmath>

using namespace std;

struct node {
    int val;
    node *left;
    node * right;
};


int main()
{
    std::ios::sync_with_stdio(false);
    cin.tie(0);

    return 0;
}